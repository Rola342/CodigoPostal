MONGO_URI_DEV="uri de mongo en desarrollo"
MONGO_URI="uri de mongo en producción"
PORT="8000"
NODE_ENV="development"
MIGRATE="nos dice si la base de datos tiene que ser migrada"
# Por ejemplo
HOST="http://localhost:8000"
